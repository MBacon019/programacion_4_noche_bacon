import 'package:flutter/material.dart';

class BienvenidaConsultorio extends StatelessWidget {
  const BienvenidaConsultorio({super.key});
  @override
  Widget build(BuildContext context) =>
      const Text('Consultorio San Rafael', style: TextStyle(fontSize: 32));
}
